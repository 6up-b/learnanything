---
title: "Config - error_impacts"
status: "current"
doc_version: "1.0"
architecture_version: "post-refactor"
source_commit: "62fd1f6404cc3a3007c6f214ba9429c45ef0114f"
source_commit_timestamp: "2026-08-17T12:05:21-04:00"
last_verified: "2026-08-18"
aliases:
  - "learnloop.toml error_impacts"
  - "[error_impacts] configuration"
config_schema_version: 2
algorithm_version: "mvp-0.9"
generated: true
field_count: 9
field_status_counts: {"ACTIVE": 6, "COMPAT": 3}
source_paths:
  - "src/learnloop/config/schema.py"
  - "src/learnloop/config/template.py"
  - "tests/test_config_refactor.py"
  - "src/learnloop/params/parameter_registry.py"
  - "tests/helpers.py"
  - "tests/test_activity_patterns.py"
  - "tests/test_activity_substrate.py"
  - "tests/test_ai_config.py"
  - "tests/test_anti_double_count.py"
  - "tests/test_assessment_contracts.py"
  - "tests/test_assessment_enforcement.py"
  - "tests/test_authoring_context.py"
  - "tests/test_blueprint_projection.py"
  - "tests/test_calibration.py"
  - "tests/test_causal_attribution_exhibit.py"
  - "tests/test_causal_attribution_p0.py"
tags:
  - "learnloop/configuration/fields"
  - "learnloop/configuration/section/error-impacts"
  - "learnloop/status/active"
---

# `error_impacts` configuration

> [!info] Effective new-vault values
> This catalog was generated by parsing the current decision-only template and validating it through `LearnLoopConfig`. It contains 9 leaf values under `error_impacts`.

The main configuration contract, precedence rules, and safe-edit workflow live in [[Configuration]]. Provider semantics live in [[AI Architecture]], and learning-policy meaning belongs in [[Learning System]]. ^config-section-scope

> [!abstract] Status vocabulary
> **ACTIVE** is canonical live policy; **DORMANT** is implemented but firewalled/default-inert; **COMPAT** is accepted only at a compatibility seam; **LEGACY** is consumed only by a frozen historical path. Status describes runtime authority, not whether the effective value happens to be false or null.

## Field catalog

| Dotted path | Effective value | Shape | Origin | Function | Runtime/refactor status | Consumer/source anchors |
|---|---|---|---|---|---|---|
| `error_impacts.recall_failure.families.recall` | `-0.25` | number | modeled default or validator seed | Sets the legacy recall-family impact recorded for a recall failure classification. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:990`; `src/learnloop/params/parameter_registry.py:1127` |
| `error_impacts.recall_failure.lo_mastery_delta` | `-0.05` | number | modeled default or validator seed | Sets learning-object mastery delta applied when an attempt is classified as recall failure. | **COMPAT** — Retained for legacy mastery-impact compatibility; current coverage uses local_severity_gain. | `src/learnloop/config/schema.py:991` |
| `error_impacts.recall_failure.local_severity_gain` | `0.8` | number | modeled default or validator seed | Sets local severity gain applied when an attempt is classified as recall failure. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:992` |
| `error_impacts.scaffold_failure.families.recall` | `-0.35` | number | modeled default or validator seed | Sets the legacy recall-family impact recorded for a scaffold failure classification. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:990`; `src/learnloop/params/parameter_registry.py:1127` |
| `error_impacts.scaffold_failure.lo_mastery_delta` | `-0.05` | number | modeled default or validator seed | Sets learning-object mastery delta applied when an attempt is classified as scaffold failure. | **COMPAT** — Retained for legacy mastery-impact compatibility; current coverage uses local_severity_gain. | `src/learnloop/config/schema.py:991` |
| `error_impacts.scaffold_failure.local_severity_gain` | `1.5` | number | modeled default or validator seed | Sets local severity gain applied when an attempt is classified as scaffold failure. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:992` |
| `error_impacts.arithmetic_slip.families.numeric` | `-0.05` | number | modeled default or validator seed | Sets the legacy numeric-family impact recorded for a arithmetic slip classification. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:990`; `src/learnloop/params/parameter_registry.py:1127` |
| `error_impacts.arithmetic_slip.lo_mastery_delta` | `0.0` | number | modeled default or validator seed | Sets learning-object mastery delta applied when an attempt is classified as arithmetic slip. | **COMPAT** — Retained for legacy mastery-impact compatibility; current coverage uses local_severity_gain. | `src/learnloop/config/schema.py:991` |
| `error_impacts.arithmetic_slip.local_severity_gain` | `0.35` | number | modeled default or validator seed | Sets local severity gain applied when an attempt is classified as arithmetic slip. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:992` |

## Compatibility-only inputs

> [!warning] Accepted is not canonical
> These spellings exist only so older vaults open. Normalization is one-way; effective configuration and runtime modules use the canonical target or discard the retired input.

| Accepted legacy input | Canonical result | Status | Function |
|---|---|---|---|
| `error_impacts.*.max_sharpening` | `recall_coverage.max_error_sharpening` | **COMPAT** | Legacy location translated only when the canonical key is absent. |

> [!warning] Omitted does not mean frozen in the file
> Most values are intentionally absent from `learnloop.toml`. Their reproducibility is protected by the defaults fingerprint keyed to `algorithms.algorithm_version`, not by dumping every default into each vault.

## Who consumes it

Every row cites the exact Pydantic field declaration (or typed dynamic-key container). Additional anchors appear only when static analysis resolves the complete dotted path, a typed submodel parameter, or an alias derived from one of those paths; a declaration-only row means no direct runtime read was resolved, not that a same-named token was accepted as evidence. The broad tests most directly mentioning this section include:

- `tests/helpers.py`
- `tests/test_activity_patterns.py`
- `tests/test_activity_substrate.py`
- `tests/test_ai_config.py`
- `tests/test_anti_double_count.py`
- `tests/test_assessment_contracts.py`
- `tests/test_assessment_enforcement.py`
- `tests/test_authoring_context.py`
- `tests/test_blueprint_projection.py`
- `tests/test_calibration.py`
- `tests/test_causal_attribution_exhibit.py`
- `tests/test_causal_attribution_p0.py`

## Extension and modification guidance

1. Add or change the typed field in `src/learnloop/config/schema.py`.
2. Add it to the minimal template only if it represents a real user decision; derived policy stays omitted.
3. If behavior changes for omitted values, make the algorithm-version/defaults-fingerprint decision explicit and update `tests/test_config_refactor.py`.
4. Put one-way legacy spelling translation in `src/learnloop/config/compat.py`; runtime modules should read only the canonical typed shape.
5. Verify the effective result with `learnloop config effective --json`.

## Related notes

- [[Configuration#Effective configuration and explicit overrides]]
- [[learnloop.toml]]
- [[Legacy Configuration Compatibility]]
- [[Environment and Machine Settings]]
