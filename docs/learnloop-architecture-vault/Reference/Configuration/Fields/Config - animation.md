---
title: "Config - animation"
status: "current"
doc_version: "1.0"
architecture_version: "post-refactor"
source_commit: "589b35df8e5e3ce56849cbdab681c6bc12737419"
source_commit_timestamp: "2026-09-03T10:26:28-07:00"
last_verified: "2026-08-18"
aliases:
  - "learnloop.toml animation"
  - "[animation] configuration"
config_schema_version: 2
algorithm_version: "mvp-0.9"
generated: true
field_count: 16
field_status_counts: {"ACTIVE": 16}
source_paths:
  - "src/learnloop/config/schema.py"
  - "src/learnloop/config/template.py"
  - "tests/test_config_refactor.py"
  - "src/learnloop/content/authoring/concept_animation.py"
  - "src/learnloop_sidecar/handlers/animation.py"
  - "src/learnloop_sidecar/handlers/settings.py"
  - "tests/test_ai_config.py"
  - "tests/test_capability_residual.py"
  - "tests/test_cli_generate_practice.py"
  - "tests/test_cli_ingest.py"
  - "tests/test_codex_output_schema.py"
  - "tests/test_concept_animation_service.py"
  - "tests/test_concept_animation_store.py"
  - "tests/test_concept_animation_validator.py"
  - "tests/test_doctor.py"
  - "tests/test_document_ir.py"
  - "tests/test_goal_intent.py"
tags:
  - "learnloop/configuration/fields"
  - "learnloop/configuration/section/animation"
  - "learnloop/status/active"
---

# `animation` configuration

> [!info] Effective new-vault values
> This catalog was generated by parsing the current decision-only template and validating it through `LearnLoopConfig`. It contains 16 leaf values under `animation`.

The main configuration contract, precedence rules, and safe-edit workflow live in [[Configuration]]. Provider semantics live in [[AI Architecture]], and learning-policy meaning belongs in [[Learning System]]. ^config-section-scope

> [!abstract] Status vocabulary
> **ACTIVE** is canonical live policy; **DORMANT** is implemented but firewalled/default-inert; **COMPAT** is accepted only at a compatibility seam; **LEGACY** is consumed only by a frozen historical path. Status describes runtime authority, not whether the effective value happens to be false or null.

## Field catalog

| Dotted path | Effective value | Shape | Origin | Function | Runtime/refactor status | Consumer/source anchors |
|---|---|---|---|---|---|---|
| `animation.enabled` | `true` | boolean | explicit template decision | Turns animation behavior on or off within consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:648`; `src/learnloop/content/authoring/concept_animation.py:686`; `src/learnloop_sidecar/handlers/animation.py:82` |
| `animation.renderer` | `"manim"` | string | explicit template decision | Controls renderer in consented concept-animation generation and sandboxed rendering, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:653`; `src/learnloop/content/authoring/concept_animation.py:697`; `src/learnloop_sidecar/handlers/animation.py:83` |
| `animation.video_resolution` | `"720p"` | string | modeled default or validator seed | Controls video resolution in consented concept-animation generation and sandboxed rendering, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:656` |
| `animation.video_aspect_ratio` | `"16:9"` | string | modeled default or validator seed | Sets the video aspect ratio used by consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:657` |
| `animation.video_generate_audio` | `false` | boolean | modeled default or validator seed | Turns video generate audio behavior on or off within consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:658` |
| `animation.video_max_shots` | `4` | integer | modeled default or validator seed | Controls video max shots in consented concept-animation generation and sandboxed rendering, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:659`; `src/learnloop/content/authoring/concept_animation.py:773`; `src/learnloop_sidecar/handlers/animation.py:98` |
| `animation.video_timeout_seconds` | `1800` | integer | modeled default or validator seed | Sets the video timeout duration in seconds for consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:661`; `src/learnloop_sidecar/handlers/animation.py:90` |
| `animation.quality` | `"qm"` | string | modeled default or validator seed | Controls quality in consented concept-animation generation and sandboxed rendering, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:664`; `src/learnloop/content/authoring/concept_animation.py:730`; `src/learnloop_sidecar/handlers/settings.py:121` |
| `animation.timeout_seconds` | `600` | integer | modeled default or validator seed | Sets the timeout duration in seconds for consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:665`; `src/learnloop/content/authoring/concept_animation.py:1070`; `src/learnloop_sidecar/handlers/animation.py:92` |
| `animation.min_duration_seconds` | `30` | integer | modeled default or validator seed | Sets the minimum duration seconds required by consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:668`; `src/learnloop_sidecar/handlers/settings.py:123` |
| `animation.max_duration_seconds` | `60` | integer | modeled default or validator seed | Caps duration seconds allowed by consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:669`; `src/learnloop/content/authoring/concept_animation.py:784`; `src/learnloop_sidecar/handlers/settings.py:124` |
| `animation.latex_enabled` | `false` | boolean | modeled default or validator seed | Turns latex behavior on or off within consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:671`; `src/learnloop/content/authoring/concept_animation.py:858` |
| `animation.auto_repair` | `true` | boolean | modeled default or validator seed | Turns auto repair behavior on or off within consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:673`; `src/learnloop/content/authoring/concept_animation.py:1074` |
| `animation.manim_executable` | `null` | nullable | modeled default or validator seed | Controls manim executable in consented concept-animation generation and sandboxed rendering, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:675`; `src/learnloop/content/authoring/concept_animation.py:1071` |
| `animation.venv_path` | `null` | nullable | modeled default or validator seed | Locates the venv resource used by consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:681` |
| `animation.auto_provision_venv` | `false` | boolean | modeled default or validator seed | Turns auto provision venv behavior on or off within consented concept-animation generation and sandboxed rendering. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:685` |

> [!warning] Omitted does not mean frozen in the file
> Most values are intentionally absent from `learnloop.toml`. Their reproducibility is protected by the defaults fingerprint keyed to `algorithms.algorithm_version`, not by dumping every default into each vault.

## Who consumes it

Every row cites the exact Pydantic field declaration (or typed dynamic-key container). Additional anchors appear only when static analysis resolves the complete dotted path, a typed submodel parameter, or an alias derived from one of those paths; a declaration-only row means no direct runtime read was resolved, not that a same-named token was accepted as evidence. The broad tests most directly mentioning this section include:

- `tests/test_ai_config.py`
- `tests/test_capability_residual.py`
- `tests/test_cli_generate_practice.py`
- `tests/test_cli_ingest.py`
- `tests/test_codex_output_schema.py`
- `tests/test_concept_animation_service.py`
- `tests/test_concept_animation_store.py`
- `tests/test_concept_animation_validator.py`
- `tests/test_config_refactor.py`
- `tests/test_doctor.py`
- `tests/test_document_ir.py`
- `tests/test_goal_intent.py`

## Extension and modification guidance

1. Add or change the typed field in `src/learnloop/config/schema.py`.
2. Add it to the minimal template only if it represents a real user decision; derived policy stays omitted.
3. If behavior changes for omitted values, make the algorithm-version/defaults-fingerprint decision explicit and update `tests/test_config_refactor.py`.
4. Put one-way legacy spelling translation in `src/learnloop/config/compat.py`; runtime modules should read only the canonical typed shape.
5. Verify the effective result with `learnloop config effective --json`.

## Related notes

- [[Configuration#Effective configuration and explicit overrides]]
- [[learnloop.toml]]
- [[Legacy Configuration Compatibility]]
- [[Environment and Machine Settings]]
