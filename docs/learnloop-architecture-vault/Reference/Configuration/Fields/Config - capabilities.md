---
title: "Config - capabilities"
status: "current"
doc_version: "1.0"
architecture_version: "post-refactor"
source_commit: "589b35df8e5e3ce56849cbdab681c6bc12737419"
source_commit_timestamp: "2026-09-03T10:26:28-07:00"
last_verified: "2026-08-18"
aliases:
  - "learnloop.toml capabilities"
  - "[capabilities] configuration"
config_schema_version: 2
algorithm_version: "mvp-0.9"
generated: true
field_count: 6
field_status_counts: {"DORMANT": 6}
source_paths:
  - "src/learnloop/config/schema.py"
  - "src/learnloop/config/template.py"
  - "tests/test_config_refactor.py"
  - "src/learnloop/substrate/canonical_projection.py"
  - "tests/test_capability_residual.py"
tags:
  - "learnloop/configuration/fields"
  - "learnloop/configuration/section/capabilities"
  - "learnloop/status/active"
---

# `capabilities` configuration

> [!info] Effective new-vault values
> This catalog was generated by parsing the current decision-only template and validating it through `LearnLoopConfig`. It contains 6 leaf values under `capabilities`.

The main configuration contract, precedence rules, and safe-edit workflow live in [[Configuration]]. Provider semantics live in [[AI Architecture]], and learning-policy meaning belongs in [[Learning System]]. ^config-section-scope

> [!abstract] Status vocabulary
> **ACTIVE** is canonical live policy; **DORMANT** is implemented but firewalled/default-inert; **COMPAT** is accepted only at a compatibility seam; **LEGACY** is consumed only by a frozen historical path. Status describes runtime authority, not whether the effective value happens to be false or null.

## Field catalog

| Dotted path | Effective value | Shape | Origin | Function | Runtime/refactor status | Consumer/source anchors |
|---|---|---|---|---|---|---|
| `capabilities.residual_activation_enabled` | `false` | boolean | modeled default or validator seed | Turns residual activation behavior on or off within optional capability-residual activation and shrinkage. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1187`; `src/learnloop/substrate/canonical_projection.py:1027` |
| `capabilities.residual_divergence_threshold` | `0.2` | number | modeled default or validator seed | Sets the decision cutoff for residual divergence in optional capability-residual activation and shrinkage. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1190`; `src/learnloop/substrate/canonical_projection.py:1030` |
| `capabilities.residual_min_independent_mass` | `2.0` | number | modeled default or validator seed | Controls residual min independent mass in optional capability-residual activation and shrinkage, using the typed value shown here as the fresh-vault default. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1193`; `src/learnloop/substrate/canonical_projection.py:1031` |
| `capabilities.residual_min_independent_groups` | `2` | integer | modeled default or validator seed | Controls residual min independent groups in optional capability-residual activation and shrinkage, using the typed value shown here as the fresh-vault default. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1194`; `src/learnloop/substrate/canonical_projection.py:1032` |
| `capabilities.residual_episode_divergence_threshold` | `0.12` | number | modeled default or validator seed | Sets the decision cutoff for residual episode divergence in optional capability-residual activation and shrinkage. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1197`; `src/learnloop/substrate/canonical_projection.py:1033` |
| `capabilities.residual_shrinkage_pseudo_count` | `4.0` | number | modeled default or validator seed | Controls residual shrinkage pseudo count in optional capability-residual activation and shrinkage, using the typed value shown here as the fresh-vault default. | **DORMANT** — Capability residuals ship behind residual_activation_enabled=false. | `src/learnloop/config/schema.py:1200`; `src/learnloop/substrate/canonical_projection.py:1034` |

> [!warning] Omitted does not mean frozen in the file
> Most values are intentionally absent from `learnloop.toml`. Their reproducibility is protected by the defaults fingerprint keyed to `algorithms.algorithm_version`, not by dumping every default into each vault.

## Who consumes it

Every row cites the exact Pydantic field declaration (or typed dynamic-key container). Additional anchors appear only when static analysis resolves the complete dotted path, a typed submodel parameter, or an alias derived from one of those paths; a declaration-only row means no direct runtime read was resolved, not that a same-named token was accepted as evidence. The broad tests most directly mentioning this section include:

- `tests/test_capability_residual.py`

## Extension and modification guidance

1. Add or change the typed field in `src/learnloop/config/schema.py`.
2. Add it to the minimal template only if it represents a real user decision; derived policy stays omitted.
3. If behavior changes for omitted values, make the algorithm-version/defaults-fingerprint decision explicit and update `tests/test_config_refactor.py`.
4. Put one-way legacy spelling translation in `src/learnloop/config/compat.py`; runtime modules should read only the canonical typed shape.
5. Verify the effective result with `learnloop config effective --json`.

## Related notes

- [[Configuration#Effective configuration and explicit overrides]]
- [[learnloop.toml]]
- [[Legacy Configuration Compatibility]]
- [[Environment and Machine Settings]]
