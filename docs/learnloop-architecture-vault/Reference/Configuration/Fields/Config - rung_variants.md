---
title: "Config - rung_variants"
status: "current"
doc_version: "1.0"
architecture_version: "post-refactor"
source_commit: "62fd1f6404cc3a3007c6f214ba9429c45ef0114f"
source_commit_timestamp: "2026-08-17T12:05:21-04:00"
last_verified: "2026-08-18"
aliases:
  - "learnloop.toml rung_variants"
  - "[rung_variants] configuration"
config_schema_version: 2
algorithm_version: "mvp-0.9"
generated: true
field_count: 8
field_status_counts: {"ACTIVE": 8}
source_paths:
  - "src/learnloop/config/schema.py"
  - "src/learnloop/config/template.py"
  - "tests/test_config_refactor.py"
  - "src/learnloop/content/authoring/rung_variants.py"
  - "src/learnloop/content/authoring/item_authoring.py"
tags:
  - "learnloop/configuration/fields"
  - "learnloop/configuration/section/rung-variants"
  - "learnloop/status/active"
---

# `rung_variants` configuration

> [!info] Effective new-vault values
> This catalog was generated by parsing the current decision-only template and validating it through `LearnLoopConfig`. It contains 8 leaf values under `rung_variants`.

The main configuration contract, precedence rules, and safe-edit workflow live in [[Configuration]]. Provider semantics live in [[AI Architecture]], and learning-policy meaning belongs in [[Learning System]]. ^config-section-scope

> [!abstract] Status vocabulary
> **ACTIVE** is canonical live policy; **DORMANT** is implemented but firewalled/default-inert; **COMPAT** is accepted only at a compatibility seam; **LEGACY** is consumed only by a frozen historical path. Status describes runtime authority, not whether the effective value happens to be false or null.

## Field catalog

| Dotted path | Effective value | Shape | Origin | Function | Runtime/refactor status | Consumer/source anchors |
|---|---|---|---|---|---|---|
| `rung_variants.easier_score_fraction` | `0.25` | number | modeled default or validator seed | Sets the easier score fraction used by learner-requested easier and harder activity variants. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:785`; `src/learnloop/content/authoring/rung_variants.py:450` |
| `rung_variants.harder_score_fraction` | `1.0` | number | modeled default or validator seed | Sets the harder score fraction used by learner-requested easier and harder activity variants. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:786`; `src/learnloop/content/authoring/rung_variants.py:450` |
| `rung_variants.self_grade_confidence` | `3` | integer | modeled default or validator seed | Controls self grade confidence in learner-requested easier and harder activity variants, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:788`; `src/learnloop/content/authoring/rung_variants.py:467` |
| `rung_variants.easier_claim_level` | `0.25` | number | modeled default or validator seed | Controls easier claim level in learner-requested easier and harder activity variants, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:789`; `src/learnloop/content/authoring/item_authoring.py:261`; `src/learnloop/content/authoring/rung_variants.py:418` |
| `rung_variants.harder_claim_level` | `0.7` | number | modeled default or validator seed | Controls harder claim level in learner-requested easier and harder activity variants, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:790`; `src/learnloop/content/authoring/item_authoring.py:261`; `src/learnloop/content/authoring/rung_variants.py:418` |
| `rung_variants.claim_pseudo_count` | `2.0` | number | modeled default or validator seed | Controls claim pseudo count in learner-requested easier and harder activity variants, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:791`; `src/learnloop/content/authoring/item_authoring.py:275`; `src/learnloop/content/authoring/rung_variants.py:429` |
| `rung_variants.max_pending_per_item` | `1` | integer | modeled default or validator seed | Caps pending per item allowed by learner-requested easier and harder activity variants. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:792`; `src/learnloop/content/authoring/rung_variants.py:392` |
| `rung_variants.retry_on_rung_violation` | `true` | boolean | modeled default or validator seed | Turns retry on rung violation behavior on or off within learner-requested easier and harder activity variants. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:793`; `src/learnloop/content/authoring/rung_variants.py:743` |

> [!warning] Omitted does not mean frozen in the file
> Most values are intentionally absent from `learnloop.toml`. Their reproducibility is protected by the defaults fingerprint keyed to `algorithms.algorithm_version`, not by dumping every default into each vault.

## Who consumes it

Every row cites the exact Pydantic field declaration (or typed dynamic-key container). Additional anchors appear only when static analysis resolves the complete dotted path, a typed submodel parameter, or an alias derived from one of those paths; a declaration-only row means no direct runtime read was resolved, not that a same-named token was accepted as evidence. The broad tests most directly mentioning this section include:

None found by exact static reference scan.

## Extension and modification guidance

1. Add or change the typed field in `src/learnloop/config/schema.py`.
2. Add it to the minimal template only if it represents a real user decision; derived policy stays omitted.
3. If behavior changes for omitted values, make the algorithm-version/defaults-fingerprint decision explicit and update `tests/test_config_refactor.py`.
4. Put one-way legacy spelling translation in `src/learnloop/config/compat.py`; runtime modules should read only the canonical typed shape.
5. Verify the effective result with `learnloop config effective --json`.

## Related notes

- [[Configuration#Effective configuration and explicit overrides]]
- [[learnloop.toml]]
- [[Legacy Configuration Compatibility]]
- [[Environment and Machine Settings]]
