---
title: "Config - ingest"
status: "current"
doc_version: "1.0"
architecture_version: "post-refactor"
source_commit: "589b35df8e5e3ce56849cbdab681c6bc12737419"
source_commit_timestamp: "2026-09-03T10:26:28-07:00"
last_verified: "2026-08-18"
aliases:
  - "learnloop.toml ingest"
  - "[ingest] configuration"
config_schema_version: 2
algorithm_version: "mvp-0.9"
generated: true
field_count: 38
field_status_counts: {"ACTIVE": 37, "COMPAT": 1}
source_paths:
  - "src/learnloop/config/schema.py"
  - "src/learnloop/config/template.py"
  - "tests/test_config_refactor.py"
  - "src/learnloop/content/pipeline/source_ingestion.py"
  - "src/learnloop/content/synthesis/source_append.py"
  - "src/learnloop/content/synthesis/source_set_synthesis.py"
  - "src/learnloop/content/pipeline/acquisition_preview.py"
  - "src/learnloop/content/pipeline/jobs.py"
  - "src/learnloop/content/sources/pdf_extraction.py"
  - "src/learnloop/cli/app.py"
  - "src/learnloop_sidecar/handlers/settings.py"
  - "src/learnloop/ingest/transcription.py"
  - "src/learnloop_sidecar/handlers/ingest.py"
  - "src/learnloop/cli/runtime.py"
  - "src/learnloop_sidecar/context.py"
  - "tests/helpers.py"
  - "tests/media_fakes.py"
  - "tests/openai_fakes.py"
  - "tests/test_agent_run_tokens.py"
  - "tests/test_agent_runs.py"
  - "tests/test_ai_config.py"
  - "tests/test_animation_media.py"
  - "tests/test_annotations.py"
  - "tests/test_apply_write_ahead.py"
  - "tests/test_attempt_ai_flow.py"
  - "tests/test_build_study_map_routing.py"
  - "tests/test_capability_residual.py"
tags:
  - "learnloop/configuration/fields"
  - "learnloop/configuration/section/ingest"
  - "learnloop/status/active"
---

# `ingest` configuration

> [!info] Effective new-vault values
> This catalog was generated by parsing the current decision-only template and validating it through `LearnLoopConfig`; the `[ingest.native]` and `ingest.audio.mode` rows were hand-edited when the per-modality native authorities replaced the retired `[ingest.native]` gates. It contains 38 leaf values under `ingest`.

The main configuration contract, precedence rules, and safe-edit workflow live in [[Configuration]]. Provider semantics live in [[AI Architecture]], and learning-policy meaning belongs in [[Learning System]]. ^config-section-scope

> [!abstract] Status vocabulary
> **ACTIVE** is canonical live policy; **DORMANT** is implemented but firewalled/default-inert; **COMPAT** is accepted only at a compatibility seam; **LEGACY** is consumed only by a frozen historical path. Status describes runtime authority, not whether the effective value happens to be false or null.

## Field catalog

| Dotted path | Effective value | Shape | Origin | Function | Runtime/refactor status | Consumer/source anchors |
|---|---|---|---|---|---|---|
| `ingest.window_char_cap` | `150000` | integer | modeled default or validator seed | Caps window char allowed by source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:775`; `src/learnloop/content/pipeline/source_ingestion.py:261` |
| `ingest.min_content_chars` | `400` | integer | modeled default or validator seed | Sets the minimum content chars required by source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:776`; `src/learnloop/content/pipeline/source_ingestion.py:223` |
| `ingest.default_goal_priority` | `0.5` | number | modeled default or validator seed | Controls default goal priority in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:777`; `src/learnloop/content/pipeline/source_ingestion.py:402` |
| `ingest.allow_auto_captions` | `false` | boolean | modeled default or validator seed | Turns allow auto captions behavior on or off within source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:778`; `src/learnloop/content/pipeline/source_ingestion.py:193` |
| `ingest.bootstrap_practice_items` | `"upfront"` | string | modeled default or validator seed | Controls bootstrap practice items in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:784`; `src/learnloop/content/synthesis/source_append.py:311`; `src/learnloop/content/synthesis/source_set_synthesis.py:1489` |
| `ingest.pdf.engine` | `"auto"` | string | explicit template decision | Controls engine in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:625`; `src/learnloop/content/pipeline/acquisition_preview.py:146`; `src/learnloop/content/pipeline/jobs.py:153` |
| `ingest.pdf.torch_device` | `""` | string | modeled default or validator seed | Controls torch device in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:628`; `src/learnloop/content/sources/pdf_extraction.py:110` |
| `ingest.pdf.force_ocr` | `false` | boolean | modeled default or validator seed | Turns force ocr behavior on or off within source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:629`; `src/learnloop/content/sources/pdf_extraction.py:90` |
| `ingest.pdf.use_llm` | `false` | boolean | modeled default or validator seed | Turns use language-model behavior on or off within source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:630`; `src/learnloop/content/pipeline/acquisition_preview.py:159`; `src/learnloop/content/sources/pdf_extraction.py:92` |
| `ingest.pdf.llm_service` | `"marker.services.openai.OpenAIService"` | string | modeled default or validator seed | Controls language-model service in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:631`; `src/learnloop/cli/app.py:1026`; `src/learnloop/content/pipeline/acquisition_preview.py:163` |
| `ingest.pdf.llm_base_url` | `""` | string | modeled default or validator seed | Controls language-model base url in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:632`; `src/learnloop/content/pipeline/acquisition_preview.py:164`; `src/learnloop/content/sources/pdf_extraction.py:96` |
| `ingest.pdf.llm_model` | `""` | string | modeled default or validator seed | Selects the model implementation or model identifier used for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:633`; `src/learnloop/content/pipeline/acquisition_preview.py:165`; `src/learnloop/content/sources/pdf_extraction.py:98` |
| `ingest.pdf.llm_api_key_env` | `"LEARNLOOP_PDF_LLM_API_KEY"` | string | modeled default or validator seed | Controls language-model API key env in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:634`; `src/learnloop/content/sources/pdf_extraction.py:100` |
| `ingest.audio.provider` | `"openai_compatible"` | string | modeled default or validator seed | Controls provider in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **COMPAT** — Raw legacy selector retained for normalization; chat transcription routes through ai.routing.transcription. | `src/learnloop/config/schema.py:683`; `src/learnloop_sidecar/handlers/settings.py:98` |
| `ingest.audio.transcription_base_url` | `"https://api.openai.com/v1"` | string | modeled default or validator seed | Controls transcription base url in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:684`; `src/learnloop/content/pipeline/acquisition_preview.py:184`; `src/learnloop/ingest/transcription.py:80` |
| `ingest.audio.transcription_model` | `"whisper-1"` | string | modeled default or validator seed | Selects the model implementation or model identifier used for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:685`; `src/learnloop/content/pipeline/acquisition_preview.py:185`; `src/learnloop/ingest/transcription.py:113` |
| `ingest.audio.transcription_api_key_env` | `"LEARNLOOP_TRANSCRIPTION_API_KEY"` | string | modeled default or validator seed | Controls transcription API key env in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:686`; `src/learnloop/ingest/transcription.py:68`; `src/learnloop_sidecar/handlers/settings.py:101` |
| `ingest.audio.language` | `""` | string | modeled default or validator seed | Controls language in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:688` |
| `ingest.audio.timeout_seconds` | `600` | integer | modeled default or validator seed | Sets the timeout duration in seconds for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:689`; `src/learnloop/ingest/transcription.py:81` |
| `ingest.audio.max_file_mb` | `25` | integer | modeled default or validator seed | Caps file megabytes allowed by source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:691` |
| `ingest.audio.mode` | `"transcription"` | string | explicit template decision | Selects the audio path: `transcription` (the transcription route or endpoint) or `native` (mp3/wav sent to the canonical_ingest chat provider as input_audio; the provider must declare `audio`). | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py`; `src/learnloop/ai/native_media.py`; `src/learnloop/content/pipeline/jobs.py` |
| `ingest.native.max_pdf_mb` | `32` | integer | modeled default or validator seed | Caps PDF megabytes sent to the chat provider by native PDF ingestion (base64 inflates ~33% inside a chat body). | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py`; `src/learnloop/content/pipeline/jobs.py` (`_extract_pdf_native`) |
| `ingest.native.fallback_when_unavailable` | `false` | boolean | modeled default or validator seed | When a native modality is selected but the routed provider cannot take it for a configuration reason, run the non-native path with a health flag instead of failing closed. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py`; `src/learnloop/content/pipeline/jobs.py` (`_decide_native`) |
| `ingest.native.max_audio_mb` | `20` | integer | modeled default or validator seed | Caps audio megabytes allowed by source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:708`; `src/learnloop/content/pipeline/jobs.py:385` |
| `ingest.budgets.inventory_input_tokens` | `20000` | integer | modeled default or validator seed | Controls inventory input tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:716`; `src/learnloop/cli/app.py:495`; `src/learnloop_sidecar/handlers/ingest.py:959` |
| `ingest.budgets.inventory_output_tokens` | `3000` | integer | modeled default or validator seed | Controls inventory output tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:717`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_shard_input_tokens` | `40000` | integer | modeled default or validator seed | Controls synthesis shard input tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:718`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_shard_output_tokens` | `10000` | integer | modeled default or validator seed | Controls synthesis shard output tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:719`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_total_input_ceiling` | `48000` | integer | modeled default or validator seed | Controls synthesis total input ceiling in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:720`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_output_tokens` | `16000` | integer | modeled default or validator seed | Controls synthesis output tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:721`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.append_neighborhood_input_tokens` | `24000` | integer | modeled default or validator seed | Controls append neighborhood input tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:722`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.append_output_tokens` | `10000` | integer | modeled default or validator seed | Controls append output tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:723`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_span_request_max_count` | `12` | integer | modeled default or validator seed | Controls synthesis span request max count in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:725`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.synthesis_span_char_cap` | `4000` | integer | modeled default or validator seed | Caps synthesis span char allowed by source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:726`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.budgets.quick_add_scope_input_tokens` | `40000` | integer | modeled default or validator seed | Controls quick add scope input tokens in source acquisition, extraction, media handling, token budgets, and durable workers, using the typed value shown here as the fresh-vault default. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:730`; `src/learnloop_sidecar/handlers/settings.py:103` |
| `ingest.runner.lease_ttl_seconds` | `120` | integer | modeled default or validator seed | Sets the lease time-to-live duration in seconds for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:752`; `src/learnloop/cli/app.py:324`; `src/learnloop/cli/runtime.py:430` |
| `ingest.runner.heartbeat_interval_seconds` | `15` | integer | modeled default or validator seed | Sets the heartbeat interval duration in seconds for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:753`; `src/learnloop_sidecar/context.py:57` |
| `ingest.runner.poll_interval_seconds` | `1.0` | number | modeled default or validator seed | Sets the poll interval duration in seconds for source acquisition, extraction, media handling, token budgets, and durable workers. | **ACTIVE** — Declared by the canonical typed configuration; exact runtime consumers are cited when a statically resolvable path exists. | `src/learnloop/config/schema.py:754`; `src/learnloop_sidecar/context.py:58` |

## Compatibility-only inputs

> [!warning] Accepted is not canonical
> These spellings exist only so older vaults open. Normalization is one-way; effective configuration and runtime modules use the canonical target or discard the retired input.

| Accepted legacy input | Canonical result | Status | Function |
|---|---|---|---|
| `ingest.audio.provider = openrouter` | `ai.providers.openrouter_transcription + ai.routing.transcription` | **COMPAT** | Moves the legacy audio-provider selector into typed AI routing. |
| `ingest.budgets.evidence_span_input_tokens` | `discarded` | **COMPAT** | Retired ingestion budget accepted only so old vaults still open. |

> [!warning] Omitted does not mean frozen in the file
> Most values are intentionally absent from `learnloop.toml`. Their reproducibility is protected by the defaults fingerprint keyed to `algorithms.algorithm_version`, not by dumping every default into each vault.

## Who consumes it

Every row cites the exact Pydantic field declaration (or typed dynamic-key container). Additional anchors appear only when static analysis resolves the complete dotted path, a typed submodel parameter, or an alias derived from one of those paths; a declaration-only row means no direct runtime read was resolved, not that a same-named token was accepted as evidence. The broad tests most directly mentioning this section include:

- `tests/helpers.py`
- `tests/media_fakes.py`
- `tests/openai_fakes.py`
- `tests/test_agent_run_tokens.py`
- `tests/test_agent_runs.py`
- `tests/test_ai_config.py`
- `tests/test_animation_media.py`
- `tests/test_annotations.py`
- `tests/test_apply_write_ahead.py`
- `tests/test_attempt_ai_flow.py`
- `tests/test_build_study_map_routing.py`
- `tests/test_capability_residual.py`

## Extension and modification guidance

1. Add or change the typed field in `src/learnloop/config/schema.py`.
2. Add it to the minimal template only if it represents a real user decision; derived policy stays omitted.
3. If behavior changes for omitted values, make the algorithm-version/defaults-fingerprint decision explicit and update `tests/test_config_refactor.py`.
4. Put one-way legacy spelling translation in `src/learnloop/config/compat.py`; runtime modules should read only the canonical typed shape.
5. Verify the effective result with `learnloop config effective --json`.

## Related notes

- [[Configuration#Effective configuration and explicit overrides]]
- [[learnloop.toml]]
- [[Legacy Configuration Compatibility]]
- [[Environment and Machine Settings]]
